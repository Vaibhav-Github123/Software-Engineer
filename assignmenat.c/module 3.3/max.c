#include <stdio.h>
int main()
{

    int i, max;
    int a[6] = {14, 15, 16, 12, 13, 186};
    max = a[0];
    for (i = 0; i < 6; i++)
    {
        if (max = a[i])
        {
            max = a[i];
        }
    }
    printf("maximum value of array an %d", max);

    return 0;
}